#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Create/recreate virtual environment
echo "Recreating virtual environment..."
rm -rf .venv
uv venv

# Install dependencies from pyproject.toml (dependencies only, no package build)
echo "Installing dependencies from pyproject.toml..."
uv sync

echo "Setup complete!"
