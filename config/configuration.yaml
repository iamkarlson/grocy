default_config:

# Enable remote debugging
debugpy:
  host: localhost
  port: 5678
  start: true
  wait: false

logger:
  default: info
  logs:
    custom_components.grocy: debug
    pygrocy.grocy_api_client: debug
